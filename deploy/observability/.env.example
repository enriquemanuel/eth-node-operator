# Copy to .env and fill in values before deploying
# Used by: deploy/observability/docker-compose.observability.yml

# ── Node identity ───────────────────────────────────────────
NODE_NAME=bare-metal-01
CLUSTER_NAME=mainnet-validators
ENVIRONMENT=production

# ── Mimir remote write ──────────────────────────────────────
# For Grafana Cloud: https://<instance>.grafana.net/api/prom/push
# For self-hosted:   http://mimir.monitoring.svc:9009/api/v1/push
MIMIR_REMOTE_WRITE_URL=https://prometheus-prod-XX-prod-XX.grafana.net/api/prom/push
MIMIR_USERNAME=123456
MIMIR_PASSWORD=glc_xxxxxxxx

# ── Loki ───────────────────────────────────────────────────
# For Grafana Cloud: https://logs-prod-XX.grafana.net/loki/api/v1/push
# For self-hosted:   http://loki.monitoring.svc:3100/loki/api/v1/push
LOKI_URL=https://logs-prod-XX.grafana.net/loki/api/v1/push
LOKI_USERNAME=123456
LOKI_PASSWORD=glc_xxxxxxxx

# ── Traefik TLS (Cloudflare DNS-01 challenge) ───────────────
DOMAIN=example.com
ACME_EMAIL=ops@example.com
CF_DNS_API_TOKEN=your_cloudflare_api_token

# ── Traefik ports ───────────────────────────────────────────
TRAEFIK_WEB_PORT=443
TRAEFIK_WEB_HTTP_PORT=80

# ── ethereum-metrics-exporter ───────────────────────────────
# Adjust if your EL/CL ports differ
EL_RPC_URL=http://localhost:8545
CL_REST_URL=http://localhost:5052
